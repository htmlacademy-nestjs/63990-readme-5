import { SortDirection, SortProperty } from '@project/shared/types';

export const MAX_POST_LIMIT = 10;
export const DEFAULT_POST_COUNT_LIMIT = 10;
export const DEFAULT_SORT_DIRECTION = SortDirection.Desc;
export const DEFAULT_PAGE_COUNT = 1;
export const DEFAULT_SORT_PROPERTY = SortProperty.CreatedAt;
export const SEARCH_POST_COUNT_LIMIT = 20;